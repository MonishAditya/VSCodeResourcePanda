@isTest
public class SubtractionTest {
	@isTest
    
     public static void postiveIntegerSub()
    {
        Integer pos = Subtraction.subtraction(7, 7);
        System.assertEquals(0, pos);
    }
}